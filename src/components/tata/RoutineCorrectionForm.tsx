import type { TimelineEntry } from "@/lib/journey";
import { correctRoutineRecordAction } from "@/lib/routine-correction";

function saoPauloInput(date: Date) { const parts = new Intl.DateTimeFormat("sv-SE", { timeZone: "America/Sao_Paulo", year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", hour12: false }).formatToParts(date); const get = (type: Intl.DateTimeFormatPartTypes) => parts.find((p) => p.type === type)?.value; return `${get("year")}-${get("month")}-${get("day")}T${get("hour")}:${get("minute")}`; }
export function RoutineCorrectionForm({ entry, childId }: { entry: TimelineEntry; childId: string }) {
  if (!entry.editable || !entry.id || !entry.entity) return null;
  return <details className="mt-2"><summary className="min-h-11 inline-flex cursor-pointer items-center text-xs font-bold text-tata-blue-dark">⋯ Corrigir registro</summary><form action={correctRoutineRecordAction} className="mt-2 rounded-2xl border border-tata-border bg-tata-surface-alt p-3 flex flex-col gap-3"><input type="hidden" name="id" value={entry.id}/><input type="hidden" name="entity" value={entry.entity}/><input type="hidden" name="childId" value={childId}/><label className="text-xs font-semibold text-tata-ink">Novo horário<input name="newTime" type="datetime-local" required defaultValue={saoPauloInput(entry.time)} className="mt-1 min-h-11 w-full rounded-xl border border-tata-border bg-white px-3"/></label><label className="text-xs font-semibold text-tata-ink">Motivo da correção<textarea name="reason" required minLength={8} rows={2} className="mt-1 w-full rounded-xl border border-tata-border bg-white p-3" placeholder="Ex.: horário lançado incorretamente"/></label><p className="text-[11px] text-tata-ink-muted">Esta alteração será registrada no histórico de auditoria.</p><button className="min-h-11 rounded-xl bg-tata-blue px-4 text-xs font-bold text-white">SALVAR CORREÇÃO</button></form></details>;
}
